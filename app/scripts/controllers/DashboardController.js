/*global app */
'use strict';

app.controller('DashboardController', ['$scope', '$location', function ($scope, $location) {
	
	$scope.init = function(){

	};

	// chama a funcao assim que abrir a tela
	$scope.init();
}]);